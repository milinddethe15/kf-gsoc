{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "centraldashboard.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "centraldashboard.labels" . | nindent 4 }}
{{- end }}
